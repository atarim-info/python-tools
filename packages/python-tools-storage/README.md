# python-tools-storage (stub)

Reserved distribution — implemented in **Phase P3**. Import root `python_tools.storage`.

Planned responsibility (PT-11): S3/MinIO wrapper (SSE, presigned URLs, safe key naming),
declarative Mongo index specs, and PgBouncer-friendly Postgres engine settings
(`sslmode=verify-full`, no server-side prepared statements).

Depends on: `python-tools-config`.

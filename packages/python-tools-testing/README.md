# python-tools-testing (stub)

Reserved distribution — implemented in **Phase P3**. Import root `python_tools.testing`.

Planned responsibility (PT-13): pytest fixtures (testcontainers for Mongo/MinIO/Kafka),
fake auth principals + fake LLM provider, freeze-time helpers, and contract-test base
classes covering both operating modes.

Depends on: `python-tools-fastapi`, `python-tools-events`, `python-tools-storage`.

"""python-tools-testing — RESERVED STUB (implemented in Phase P3).

Planned (PT-13): pytest fixtures with testcontainers for Mongo/MinIO/Kafka
(session-scoped, reusable), fake auth principals (seeker/admin/service), a fake LLM
provider, freeze-time helpers, and contract-test base classes so consumers can test
both operating modes with one suite.

Importing this package is safe; it exposes no functionality yet.
"""

from __future__ import annotations

__all__: list[str] = []
__version__ = "0.0.0"
__status__ = "stub"

"""python-tools-fastapi — RESERVED STUB (implemented in Phase P2). Import root ``python_tools.web``.

Planned (PT-07): an app factory that wires logging + observability in one call, the
standard error envelope ``{"error": {"code", "message", "details"}}`` and shared code
enum, request-ID / access-log / exception-mapping middleware, and helpers for cursor
pagination, ETag + ``If-Match``, and ``Idempotency-Key`` handling.

Importing this package is safe; it exposes no functionality yet.
"""

from __future__ import annotations

__all__: list[str] = []
__version__ = "0.0.0"
__status__ = "stub"

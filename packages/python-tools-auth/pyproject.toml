[project]
name = "python-tools-auth"
version = "0.0.0"
description = "STUB (later phase): UTMS JWT verification, app_scope enforcement, permission checks."
requires-python = ">=3.12"
readme = "README.md"
authors = [{ name = "Platform Engineering" }]
dependencies = [
    "python-tools-config",
    "python-tools-logging",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/python_tools"]

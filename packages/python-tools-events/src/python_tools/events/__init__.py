"""python-tools-events — RESERVED STUB (implemented in Phase P3).

Planned (PT-09): CloudEvents v1.0 envelope builders/validators, Kafka producer/consumer
wrappers (TLS + SCRAM per INF-24), topic naming, a transactional-outbox helper, and
no-op / in-memory sinks so services run without Kafka.

Importing this package is safe; it exposes no functionality yet.
"""

from __future__ import annotations

__all__: list[str] = []
__version__ = "0.0.0"
__status__ = "stub"

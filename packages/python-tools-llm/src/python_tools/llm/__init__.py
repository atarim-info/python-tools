"""python-tools-llm — RESERVED STUB (implemented in Phase P3).

Planned (PT-12): a provider port (direct LangChain vs gateway HTTP), model routing that
returns the concrete ``model_id`` before prompt assembly, a per-model prompt/instruction
pack loader with a resolution cascade (exact model_id → family → provider → default),
a structured-output helper, and a stub provider for CI.

Importing this package is safe; it exposes no functionality yet.
"""

from __future__ import annotations

__all__: list[str] = []
__version__ = "0.0.0"
__status__ = "stub"

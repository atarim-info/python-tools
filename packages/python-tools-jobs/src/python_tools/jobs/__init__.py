"""python-tools-jobs — RESERVED STUB (implemented in Phase P2).

Planned (PT-10): the async-job model and ``202`` + ``GET /jobs/{id}`` contract, a job
store protocol with Mongo and Postgres implementations, idempotent submission, and a
worker loop with claim / heartbeat / retry-with-backoff / dead-letter.

Importing this package is safe; it exposes no functionality yet.
"""

from __future__ import annotations

__all__: list[str] = []
__version__ = "0.0.0"
__status__ = "stub"
